{
  "name": "layerone",
  "private": true,
  "version": "0.0.0",
  "packageManager": "npm@10.5.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "build:tokens": "npm --workspace @layerone-theme/tokens run build",
    "build:theme": "npm --workspace @layerone-theme/theme run build",
    "lint": "echo 'lint placeholder'",
    "test": "echo 'test placeholder'",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "changeset:publish": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "typescript": "^5.4.5"
  }
}
